version: '3'
name: otaku-bot
services:
  api:
    container_name: otaku-bot
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - '4000:4000'
    volumes:
      - ./src:/app/src
    expose:
      - '4000'
